import React from "react";
import { Container, Typography } from "@mui/material";

const ContactUs = () => (
  <Container>
    <Typography variant="h2">Contact Us</Typography>
    <Typography variant="body1">This is the Contact Us page.</Typography>
  </Container>
);

export default ContactUs;